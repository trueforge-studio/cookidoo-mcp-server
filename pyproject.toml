[project]
name = "cookidoo-mcp"
version = "1.0.0"
description = "MCP server for Cookidoo recipes, shopping lists, collections, and meal planning"
requires-python = ">=3.12"
dependencies = [
  "mcp==1.0.0",
  "cookidoo-api==0.17.2",
  "aiohttp>=3.9.0",
  "python-dotenv>=1.0.0",
]
